# Reference: Workers AI Models

The UI accepts any model id that starts with `@cf/` or `@hf/`.

Default model:

- `@cf/zai-org/glm-4.7-flash`

Curated picks are also listed in the app at `/models`.

Official catalog:

- https://developers.cloudflare.com/workers-ai/models/
